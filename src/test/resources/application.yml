spring:
  kafka:
    config:
      application-id: kafka-project
      bootstrap-servers: 127.0.0.1:9092
      inbox-topic: inbox-topic
      outbox-topic: outbox-topic
      group-id: group-id
      auto-offset: earliest